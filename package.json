{
  "name": "vix-update-price",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ vix-update-price",
    "create-local": "graph create --node http://localhost:8020/ vix-update-price",
    "remove-local": "graph remove --node http://localhost:8020/ vix-update-price",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 vix-update-price",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.63.0",
    "@graphprotocol/graph-ts": "0.30.0"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}